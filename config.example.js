// Copy this file to `config.js` and fill in your Supabase project values.
// The anon/publishable key is safe to ship to the browser only if Row Level
// Security is enabled on every table (see README / CLAUDE.md for the SQL).
window.LV_CONFIG = {
  SUPABASE_URL: 'https://YOUR-PROJECT.supabase.co',
  SUPABASE_ANON_KEY: 'YOUR-ANON-OR-PUBLISHABLE-KEY',
};
